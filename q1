def count_chars(string, char):    	# создали функцию с 2мя аргументами							
index = 0                   	      # создали счетчик индекс, переменная со значением ноль							
count = 0                      	    # создали счетчик count, переменная со значением ноль							
while index < len(string):    	    # создали цикл где index меньше чем колл-во строк аргумента string							
    if string[index] == char:     	# Объявили условие, Если индекс строки string (буква стринга) ровна char							
            count = count + 1	      # то, переменная count (так как находится в внутреннем отступе условия if, должен заработать как счетчик, который увеличивается, тем самым достигает какую то цель, но не понятно какую"  						
    index = index + 1         	    # тут же счетчик index понятно увеличивается так как он записан в условии if string[index] означает, то что пройдется по каждой букве с индекса 0 до последней буквы, но не включая последнюю букву 
                                                                                                                                                      # строки ( Если включить последний символ то надо было указать не меньше < а <= )"							
return count                   	    # вернуть тот самый счетчик, который упомянут в 6й строке							
